'use strict';
(()=>{
 const G=window.GGX;if(!G)return;
 function render(a){const gate=document.getElementById('decision-gate');if(!gate||!a?.rows)return;gate.querySelector('#ggConstraintLens')?.remove();const sorted=[...a.rows].sort((x,y)=>x.ratio-y.ratio),weak=sorted[0],next=sorted[1],strong=sorted[sorted.length-1];const status=r=>r.ratio<.8?'STOP':r.ratio<1?'SHORT':r.ratio<1.15?'TIGHT':'BUFFER';const pct=r=>Math.round((r.ratio-1)*100);const box=document.createElement('section');box.id='ggConstraintLens';box.className='gg-constraint-lens';box.innerHTML=`<div class="gg-cl-head"><div><span>CONSTRAINT LENS</span><h3>Three numbers that explain the site.</h3></div><p>The full model still runs underneath. This surface keeps only the dependencies most likely to change the decision.</p></div><div class="gg-cl-grid"><article class="critical"><small>WEAKEST DEPENDENCY</small><b>${G.esc(weak.name)}</b><strong>${weak.ratio.toFixed(2)}×</strong><i>${status(weak)}</i><p>${pct(weak)>=0?`About ${pct(weak)}% risk-adjusted buffer.`:`About ${Math.abs(pct(weak))}% below modeled need.`}</p></article><article><small>NEXT CONSTRAINT</small><b>${G.esc(next.name)}</b><strong>${next.ratio.toFixed(2)}×</strong><i>${status(next)}</i><p>${next.ratio<1.15?'Close enough to matter during diligence.':'Currently outside the immediate bottleneck zone.'}</p></article><article><small>DEEPEST BUFFER</small><b>${G.esc(strong.name)}</b><strong>${strong.ratio.toFixed(2)}×</strong><i>${status(strong)}</i><p>Useful headroom, but it cannot offset failure in another system.</p></article></div>`;const swap=gate.querySelector('#ggOsSwapDelta');(swap||gate.querySelector('.ggx-gate-head'))?.insertAdjacentElement('afterend',box)}
 G.injectStyle('gg-constraint-lens-style',`.gg-survey .gg-constraint-lens{border-bottom:1px solid #c8d1d6;background:#fbfcfd;padding:18px 22px}.gg-cl-head{display:flex;justify-content:space-between;gap:28px;align-items:end}.gg-cl-head span{font:800 7px var(--sv-mono);letter-spacing:.13em;color:var(--sv-orange)}.gg-cl-head h3{margin:5px 0 0;font:900 21px/1 Arial Black,Arial,sans-serif;text-transform:uppercase;color:var(--sv-ink)}.gg-cl-head p{max-width:480px;margin:0;color:#748088;font-size:10px;line-height:1.5}.gg-cl-grid{display:grid;grid-template-columns:1.15fr 1fr 1fr;gap:1px;background:#cbd4d9;border:1px solid #cbd4d9;margin-top:14px}.gg-cl-grid article{background:#f7f9fa;padding:14px;display:grid;grid-template-columns:1fr auto;gap:4px 12px}.gg-cl-grid article.critical{background:#fff7f3}.gg-cl-grid small{grid-column:1/-1;color:#7b878e;font:800 6px var(--sv-mono);letter-spacing:.1em}.gg-cl-grid b{font-size:14px}.gg-cl-grid strong{font:800 15px var(--sv-mono);color:var(--sv-blue)}.gg-cl-grid .critical strong{color:var(--sv-orange)}.gg-cl-grid i{font:800 7px var(--sv-mono);font-style:normal;color:#65727a}.gg-cl-grid p{grid-column:1/-1;margin:5px 0 0;color:#6f7b82;font-size:9px;line-height:1.45}@media(max-width:720px){.gg-cl-head{display:block}.gg-cl-head p{margin-top:8px}.gg-cl-grid{grid-template-columns:1fr}}`);G.afterAnalysis(render);if(G.analysis())setTimeout(()=>render(G.analysis()),120)
})();