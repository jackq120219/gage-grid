'use strict';
(()=>{
  const G=window.GGX;if(!G)return;
  const axes=[['POWER','power',70],['WATER','water',5],['WASTE','waste',3.5],['GAS','gas',420],['FIBER','fiber',3]];
  function points(values,cx=150,cy=150,r=112){return axes.map(([,key,max],i)=>{const a=(-Math.PI/2)+(Math.PI*2*i/axes.length),v=Math.max(.08,Math.min(1,(Number(values[key])||0)/max));return`${(cx+Math.cos(a)*r*v).toFixed(1)},${(cy+Math.sin(a)*r*v).toFixed(1)}`}).join(' ')}
  function ringPoints(scale,cx=150,cy=150,r=112){return axes.map((_,i)=>{const a=(-Math.PI/2)+(Math.PI*2*i/axes.length);return`${(cx+Math.cos(a)*r*scale).toFixed(1)},${(cy+Math.sin(a)*r*scale).toFixed(1)}`}).join(' ')}
  function labels(){return axes.map(([label],i)=>{const a=(-Math.PI/2)+(Math.PI*2*i/axes.length),x=150+Math.cos(a)*138,y=150+Math.sin(a)*138;return`<text x="${x.toFixed(1)}" y="${y.toFixed(1)}" text-anchor="middle" dominant-baseline="middle">${label}</text>`}).join('')}
  function install(){
    const field=document.getElementById('ggOsHeroField');if(!field||document.getElementById('ggSurveyPlot'))return;
    const host=document.createElement('div');host.id='ggSurveyPlot';host.className='gg-survey-plot';host.innerHTML=`<div class="gg-survey-plot-head"><span>PROJECT / SITE OVERLAY</span><b id="ggSurveyPlotState">Awaiting screen</b></div><svg viewBox="0 0 300 300" role="img" aria-label="Project load and site capacity survey plot"><polygon class="ring outer" points="${ringPoints(1)}"></polygon><polygon class="ring mid" points="${ringPoints(.66)}"></polygon><polygon class="ring inner" points="${ringPoints(.33)}"></polygon>${axes.map((_,i)=>{const a=(-Math.PI/2)+(Math.PI*2*i/axes.length);return`<line x1="150" y1="150" x2="${(150+Math.cos(a)*112).toFixed(1)}" y2="${(150+Math.sin(a)*112).toFixed(1)}"></line>`}).join('')}<polygon id="ggSurveyCapacity" class="capacity" points="${ringPoints(.08)}"></polygon><polygon id="ggSurveyDemand" class="demand" points="${ringPoints(.08)}"></polygon>${labels()}<circle cx="150" cy="150" r="3" class="datum"></circle></svg><div class="gg-survey-legend"><span><i class="demand"></i>PROJECT LOAD</span><span><i class="capacity"></i>SITE CAPACITY</span></div>`;
    field.appendChild(host);
    const updateReq=()=>{const r=G.current();if(!r)return;const demand=document.getElementById('ggSurveyDemand');if(demand)demand.setAttribute('points',points(r));};
    ['powerReq','waterReq','wasteReq','gasReq','fiberReq','projectType'].forEach(id=>document.getElementById(id)?.addEventListener('input',updateReq));['fiberReq','projectType'].forEach(id=>document.getElementById(id)?.addEventListener('change',()=>setTimeout(updateReq,10)));updateReq();
  }
  function update(a){if(!a)return;const cap=document.getElementById('ggSurveyCapacity'),state=document.getElementById('ggSurveyPlotState');if(cap)cap.setAttribute('points',points(a.s));const req=document.getElementById('ggSurveyDemand');if(req)req.setAttribute('points',points(a.req));if(state){state.textContent=`${a.s.name} · ${a.worst.name} ${a.worst.ratio.toFixed(2)}×`;state.dataset.state=a.vclass}}
  G.injectStyle('gg-survey-plot-style',`
    .gg-survey .gg-os-field-lines,.gg-survey .gg-os-field-label,.gg-survey .gg-os-site-core,.gg-survey .gg-os-scan{opacity:.14!important}.gg-survey-plot{position:absolute;inset:52px 24px 22px;z-index:5;display:grid;grid-template-rows:auto 1fr auto;pointer-events:none}.gg-survey-plot-head{display:flex;justify-content:space-between;gap:14px;align-items:center}.gg-survey-plot-head span{font:800 7px var(--sv-mono);letter-spacing:.13em;color:var(--sv-orange)}.gg-survey-plot-head b{font:700 8px var(--sv-mono);color:#5e6b72}.gg-survey-plot-head b[data-state="hold"]{color:var(--sv-red)}.gg-survey-plot-head b[data-state="conditional"]{color:var(--sv-yellow)}.gg-survey-plot svg{width:100%;height:100%;min-height:300px;overflow:visible}.gg-survey-plot .ring{fill:none;stroke:#c8d1d6;stroke-width:.8;stroke-dasharray:3 4}.gg-survey-plot line{stroke:#d3dade;stroke-width:.7}.gg-survey-plot text{font:700 7px var(--sv-mono);letter-spacing:.08em;fill:#75838a}.gg-survey-plot .demand{fill:rgba(255,90,54,.09);stroke:var(--sv-orange);stroke-width:2;vector-effect:non-scaling-stroke;transition:points .45s cubic-bezier(.2,.8,.2,1)}.gg-survey-plot .capacity{fill:rgba(47,85,217,.08);stroke:var(--sv-blue);stroke-width:1.6;stroke-dasharray:8 4;vector-effect:non-scaling-stroke;transition:points .55s cubic-bezier(.2,.8,.2,1);animation:ggSurveyDash 18s linear infinite}.gg-survey-plot .datum{fill:var(--sv-ink)}.gg-survey-legend{display:flex;justify-content:center;gap:20px;color:#718087;font:700 7px var(--sv-mono);letter-spacing:.08em}.gg-survey-legend span{display:flex;align-items:center;gap:6px}.gg-survey-legend i{width:18px;height:6px;display:block;border:1px solid}.gg-survey-legend i.demand{border-color:var(--sv-orange);background:rgba(255,90,54,.09)}.gg-survey-legend i.capacity{border-color:var(--sv-blue);background:rgba(47,85,217,.08)}@keyframes ggSurveyDash{to{stroke-dashoffset:-120}}@media(prefers-reduced-motion:reduce){.gg-survey-plot .capacity{animation:none}}@media(max-width:1100px){.gg-survey-plot{inset:48px 18px 20px}.gg-survey-plot svg{min-height:260px}}
  `);
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
  G.afterAnalysis(a=>update(a));
})();