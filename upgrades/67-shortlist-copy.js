'use strict';
(()=>{
 const G=window.GGX;if(!G)return;
 function mount(){const ranking=document.getElementById('ranking');if(!ranking||document.getElementById('ggShortlistCopy'))return;const btn=document.createElement('button');btn.id='ggShortlistCopy';btn.type='button';btn.className='gg-shortlist-copy';btn.textContent='COPY TOP-3 SHORTLIST';ranking.insertAdjacentElement('afterbegin',btn);btn.onclick=async()=>{const ranked=G.rank().slice(0,3);if(!ranked.length){G.toast('Run a project screen first');return}const text=['Gage Grid — Project Shortlist',...ranked.map((a,i)=>`${i+1}. ${a.s.name} — ${a.verdict} — ${a.low}-${a.high} — weakest: ${a.worst?.name||'unknown'} (${Math.round((a.worst?.ratio||0)*100)}%)`),'Screening output only; verify parcel-level utility facts in writing.'].join('\n');await G.copy(text)}}
 G.injectStyle('gg-shortlist-copy-css',`.gg-shortlist-copy{float:right;margin:0 0 8px 8px;border:1px solid #c7cdd1;background:#fff;padding:7px 9px;font:800 8px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.05em;cursor:pointer}.gg-shortlist-copy:hover{border-color:#2f55d9;color:#2f55d9}`);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount()
})();

(()=>{
 const G=window.GGX;if(!G)return;function mount(){const button=document.getElementById('analyseBtn'),inputs=document.querySelector('.inputs');if(!button||!inputs||document.getElementById('ggCopyRequirements'))return;const copy=document.createElement('button');copy.id='ggCopyRequirements';copy.type='button';copy.className='gg-copy-requirements';copy.textContent='COPY PROJECT REQUIREMENTS';button.insertAdjacentElement('afterend',copy);copy.onclick=async()=>{const r=G.current();if(!r){G.toast('Project inputs are not ready');return}const site=document.getElementById('siteSelect')?.selectedOptions?.[0]?.textContent?.trim()||'Selected site';const project=document.getElementById('projectType')?.selectedOptions?.[0]?.textContent?.trim()||r.projectType;const text=[`Gage Grid — Project Requirement Brief`,`Project: ${project}`,`Screening site: ${site}`,`Peak power: ${r.power} MW`,`Water: ${r.water} MGD`,`Wastewater: ${r.waste} MGD`,`Natural gas: ${r.gas} MMBtu/h`,`Fiber requirement: Level ${r.fiber}`,`Power redundancy: Level ${r.redundancy}`,`24-month load growth: ${Math.round((r.growth||0)*100)}%`,`Needed online: ${r.timeline} months`,`Risk posture: ${r.risk}`,`Pre-development capital exposure: ${new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(r.capital||0)}`,'Screening assumptions only; confirm parcel-level utility facts before commitment.'].join('\n');const ok=await G.copy(text);if(ok){copy.textContent='REQUIREMENTS COPIED ✓';setTimeout(()=>copy.textContent='COPY PROJECT REQUIREMENTS',1700)}};G.injectStyle('gg-copy-requirements-css',`.gg-copy-requirements{width:100%;margin-top:7px;border:1px solid #464c41;background:transparent;color:#b9c0b2;padding:9px 10px;font:800 8px ui-monospace,SFMono-Regular,Menlo,monospace;letter-spacing:.07em;cursor:pointer}.gg-copy-requirements:hover{border-color:#caff42;color:#caff42}`)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount();
})();